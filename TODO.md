- [x] Remove “Commercial grade construction” from all product text on the product page
  - [x] Edit `src/pages/Products.tsx` to sanitize `product.description` and `product.features`
  - [x] Ensure UI renders sanitized text (no double spaces)
  - [ ] Run `npm run dev` (or build/lint) to verify no runtime/type errors
